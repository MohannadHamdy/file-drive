/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "yjhgikbkecrqotvahyki.supabase.co",
      },
    ],
  },
};

export default nextConfig;
